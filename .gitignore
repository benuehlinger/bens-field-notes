# Dependencies
node_modules/

# Next.js (legacy, not used)
.next/
*.tsbuildinfo

# Environment — never commit secrets
.env
.env.local

# macOS
.DS_Store

# Quarto build output
_site/
_freeze/
.quarto/

# Wrangler local state
worker/.wrangler/

# Cursor agent (may contain auth tokens)
.cursor/

# Python
__pycache__/
*.py[cod]
.venv/

# Analysis outputs — regenerated by running analysis/*.py against R2
analysis/outputs/
# Exception: static reference data needed for site rendering in CI
!analysis/outputs/lake_coverage.parquet

# Local notebook drafts — ignore everything except the committed skeleton + README
drafts/*
!drafts/README.md
!drafts/skeleton-post.ipynb


/.quarto/
**/*.quarto_ipynb
